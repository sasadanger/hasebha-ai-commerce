PyYAML>=6.0,<7.0
requests>=2.31,<3.0
duckdb>=1.3,<2.0
pyarrow>=16.0,<24.0
pytest>=8.0,<10.0
numpy==2.5.1
pandas==3.0.5
scikit-learn==1.9.0
matplotlib==3.11.1
catboost==1.2.10
lightgbm==4.7.0
openpyxl>=3.1,<4.0

# CommercePilot AI service (Phase 4).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
httpx>=0.27,<1.0

# Transformer screening (Phase 2C NLP model-improvement, 2026-08-11 overnight session).
# torch requires the CUDA-specific index for a GPU build; a plain
# `pip install -r requirements.txt` installs a CPU-only torch. To match the
# verified overnight environment (torch 2.6.0+cu124), install torch first with:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# then the rest of this file as normal.
transformers>=5.15,<6.0
datasets>=5.0,<6.0
accelerate>=1.14,<2.0
safetensors>=0.8,<1.0
sentencepiece>=0.2,<0.3
protobuf>=7.0,<8.0
torch>=2.6,<3.0
